---
version: 1
slug: "src-app-tsx"
primary_target: "src/App.tsx"
related_targets: ["src/ui/RadarChrome.tsx","src/styles.css"]
---

SCOPE: Production radar surface for the installable Mistr Alpha; visitor mode is Operate.
AUDIENCE: The owner and experienced storm enthusiasts monitoring live storms for long desktop sessions.
TASK: Select one NEXRAD site, inspect base reflectivity without camera/playback lag, scrub recent measured scans, and always know the displayed scan timestamp and numeric age of the painted frame.
PROOF: The existing decoded Level II custom layer, bounded resident GPU frames, paint receipts, live publication state, and recovery diagnostics remain the truth source.
DIRECTION: Stormlight Cyclorama, quiet-instrument refinement. Full-screen radar; tightly content-sized icon-led top-center toolbar in the sequence `Mistr | site icon, recenter icon | eye icon`; no left application menu or About panel; compact bounded bottom-center playback bar. The site icon owns the single canonical searchable site browser. The eye icon has the tooltip `Radar View` and opens a popup containing only `Smooth` and `Native`. Fixed Alpha product/tilt facts remain documentation rather than inert controls. A consistently quiet map uses close matte-charcoal land and water tones, keeps local roads, buildings, railways, water names, towns, and secondary labels below radar, and retains restrained coastlines, major routes, boundaries, countries, natural-case states, and important cities above it. Regional roads disclose progressively, route lines never outshine precipitation or major cities, and global radar opacity is never used as the map-readability fix. Approved comps remain composition references only: docs/design/comps/mistr-alpha-v2-a-quiet-radar.webp and mistr-alpha-v2-c-menu-open.webp; their side menu, larger chrome, stencil typography, and accent are superseded by the current design contract.
MEMORABLE MOMENT: Click a storm to place a precise reticle while the measured dBZ value appears in the stable playback bar.
CONSTRAINTS: No permanent sidebar, left application menu, full-width rails, floating panel chains, speculative toolbar controls, duplicate site picker, inert product/elevation controls, radar-tinting atmosphere, dedicated step buttons, ornamental stencil type, visible engineering alignment markers, or normal-interface prototype diagnostics. One temporary site or view panel at a time; opening it never resizes the map or moves the playback bar. Normal bottom-bar status is limited to displayed scan timestamp and numeric age alongside transport/timeline/dBZ: no visible `Fresh`, `Stale`, `Playing`, `Paused`, or `Newest`. Green age is reserved for the recent newest painted live scan; historical, archive, and old latest-live ages are white, with numeric and accessible truth independent of color. The shared weak-return presentation curve may suppress non-positive pixels and progressively reveal 0–20 dBZ, but it never changes native interrogation or claims meteorological clutter removal; `Smooth` and `Native` remain the only view controls. Before first paint and during loading, recovery, or failure, show plain-language exceptional progress/notices instead of fake timeline truth. Windows-first; compact desktop labels collapse before controls; keyboard, tooltip, focus-return, forced-colors, and reduced-motion support remain intact.
UNRESOLVED: Public Alpha still requires an owner decision on interactive unsigned-build messaging and installation instructions. Controlled Windows sleep/wake and clean-machine installer validation passed. No new product surface is implied by the remaining release decision.
